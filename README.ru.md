# Testovak 🎓

> Расширение для браузера, которое автоматизирует поиск и запись на экзамены в системе Moodle ČZU (Чешский аграрный университет).

## 🎯 Что делает расширение?

Testovak помогает студентам найти и записаться на экзамен в удобное время. Расширение автоматически:

1. **Мониторит доступные слоты** — периодически обновляет страницу и проверяет появление новых терминов
2. **Фильтрует по вашим критериям** — ищет только в указанном диапазоне дат и времени
3. **Автоматически записывает** — как только находит подходящий слот, сразу переходит на страницу записи

## 📥 Скачать

Установите Testovak из официальных магазинов:

- **[Chrome Web Store](https://chromewebstore.google.com/detail/ppniandfcnnmcgoidjoejfhehnncgohj?utm_source=item-share-cb)** - Установка для Chrome/Edge/Brave
- **[Firefox Add-ons](https://addons.mozilla.org/en-US/firefox/addon/testovak/)** - Установка для Firefox

## 🚀 Установка

### Chrome

1. Скачайте или склонируйте репозиторий
2. Выполните сборку:
   ```bash
   pnpm install
   pnpm build:chrome
   ```
3. Откройте `chrome://extensions/`
4. Включите "Режим разработчика"
5. Нажмите "Загрузить распакованное расширение"
6. Выберите папку `dist-chrome`

### Firefox

1. Скачайте или склонируйте репозиторий
2. Выполните сборку:
   ```bash
   pnpm install
   pnpm build:firefox
   ```
3. Откройте `about:debugging#/runtime/this-firefox`
4. Нажмите "Загрузить временное дополнение"
5. Выберите файл `manifest.json` из папки `dist-firefox`

## 📖 Как пользоваться

### Шаг 1: Откройте страницу с тестами

Перейдите на страницу Moodle ČZU, где отображаются доступные тесты/экзамены.

### Шаг 2: Нажмите кнопку "Хочу найти термин!"

Рядом с каждым доступным тестом появится яркая кнопка с градиентом. Нажмите на неё.

### Шаг 3: Настройте параметры поиска

В открывшемся модальном окне укажите:

- **Тест** — выберите экзамен из списка
- **Промежуток дат** — диапазон дней, в которые вы хотите сдавать
- **Промежуток времени** — часы, в которые вам удобно (например, 09:00 - 17:00)

### Шаг 4: Запустите поиск

Нажмите "Начать!" и расширение начнёт работу:

- Страница будет автоматически обновляться каждые 10 секунд
- Вы увидите прогресс-бар и время поиска
- Как только появится подходящий слот — расширение автоматически перейдёт к записи

### Шаг 5: Завершите запись

Когда расширение найдёт слот, вы окажетесь на странице подтверждения записи. Просто нажмите кнопку записи!

## 🏗️ Архитектура

```
src/
├── moodle.tsx          # Точка входа, инъекция в страницу Moodle
├── SetupModal.tsx      # Модальное окно настройки поиска
├── ProgressModal.tsx   # Модальное окно прогресса поиска
├── hooks/
│   └── useExamSearch.ts # React хук с логикой поиска
├── types.ts            # TypeScript типы
├── utils.ts            # Утилиты парсинга страниц Moodle
└── components/ui/      # UI компоненты (Radix UI + Tailwind)
```

### Как это работает внутри

#### 1. Инъекция в страницу (`moodle.tsx`)

При загрузке страницы Moodle расширение:

- Парсит DOM и находит все доступные тесты
- Добавляет кнопки "Хочу найти термин!" к каждому тесту
- Проверяет, запущен ли уже поиск (через Chrome Storage)

#### 2. Настройка поиска (`SetupModal.tsx`)

Модальное окно позволяет:

- Выбрать тест из списка доступных
- Указать диапазон дат через календарь
- Указать диапазон времени через выпадающие списки
- Увидеть уже доступные слоты (если есть)

#### 3. Процесс поиска (`useExamSearch.ts`)

Хук `useExamSearch` управляет всей логикой:

```typescript
const {
  status, // Текущий статус поиска
  elapsedTime, // Время с начала поиска
  availableDates, // Найденные даты
  parsedTimes, // Временные слоты по датам
  actions: {
    cancelSearch, // Отменить поиск
    selectTimeSlot, // Выбрать слот вручную
  },
} = useExamSearch();
```

Алгоритм поиска:

1. Загружает конфигурацию из Chrome Storage
2. Парсит доступные даты из DOM страницы
3. Фильтрует даты в пределах ±2 дня от выбранного диапазона
4. Загружает временные слоты для каждой даты
5. Ищет точное совпадение с критериями
6. Если найдено — переходит на страницу записи
7. Если нет — обновляет страницу через 10 секунд

#### 4. Парсинг страниц (`utils.ts`)

Утилиты для извлечения данных из DOM Moodle:

- `parseAvailableTests()` — парсит список тестов с датами и ссылками
- `parseAvailableTimes()` — парсит временные слоты на странице дня
- `parseTimesPage()` — загружает и парсит страницу с временными слотами

## 🛠️ Разработка

```bash
# Установка зависимостей
pnpm install

# Режим разработки (Chrome)
pnpm dev:chrome

# Режим разработки (Firefox)
pnpm dev:firefox

# Сборка для всех браузеров
pnpm build
```

## 📦 Технологии

- **React 18** — UI компоненты
- **TypeScript** — типизация
- **Tailwind CSS** — стилизация
- **Radix UI** — доступные UI примитивы
- **Vite** — сборка
- **Chrome Extension Manifest V3** — API расширения

## ⚠️ Ограничения

- Работает только на `moodle.czu.cz`
- Требует авторизации в Moodle
- Не гарантирует запись (слот может быть занят в момент перехода)
- Страница должна оставаться открытой во время поиска

## 📄 Лицензия

MIT
